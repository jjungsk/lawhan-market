# lawhan-market Nginx 리버스 프록시 뼈대
#
# M0 시점: 실제 라우팅 규칙은 M9(Docker Compose 통합)에서 작성.
# 예정된 라우팅 (docs/architecture-requirements.md §3 참고):
#   /api/*  -> backend:8080
#   그 외    -> frontend:3000

events {}

http {
    server {
        listen 80;

        # location /api/ {
        #     proxy_pass http://backend:8080;
        # }
        #
        # location / {
        #     proxy_pass http://frontend:3000;
        # }
    }
}
